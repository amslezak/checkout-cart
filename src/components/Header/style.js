import styled from "react-emotion"
import { justifyContent, space, width, borderRadius } from "styled-system"

export default styled.div`
  ${space};
  ${width};
  ${borderRadius};
  ${justifyContent};
`
